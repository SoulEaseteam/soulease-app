// REMOVED — Round 28x reverted (founder direction "ย้อนกลับไปก่อนสั่งแก้").
// Booking-flow files keep using direct `therapistsData.find(...)` static
// lookup like before. File kept as an empty stub because the sandbox
// forbids unlinking; safe to delete physically in a follow-up commit.
export {};
