// REMOVED — Round 28j (founder 2026-05-02): "language pill จาก TopNav
// แค่หน้าเดียว กดครั้งเดียวแปลภาษาทั้งเว็บ". The site-wide top-right
// floating pill was removed; the language switcher now lives only in
// TopNav (home page). One click there propagates the language across
// the whole site via i18n.
//
// File kept as an empty stub because the sandbox forbids unlinking.
// Safe to delete physically in a follow-up commit.
export {};
